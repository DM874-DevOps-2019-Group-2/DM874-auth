apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: auth
spec:
  replicas: 1
  serviceName: "auth"
  selector:
    matchLabels:
      app: auth
  updateStrategy:
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: auth
    spec:
      containers:
        - name: auth-container
          image: IMAGE
          imagePullPolicy: Always
          envFrom:
            - configMapRef:
                name: auth-config
            - secretRef:
                name: auth-secret
          resources:
            requests:
              memory: "300Mi"
              cpu: "500m"
            limits:
              memory: "600Mi"
              cpu: "1000m"